# v1.44.1｜作品頁間距 QA

核對日期：2026-09-07。本文件記錄 source 範圍與本機驗證；正式部署須於固定版本合併後獨立驗收。

## 調整範圍

- 縮短作品資料到外部參考連結，以及歌曲、資料來源分區之間的留白。
- 保留既有文字層級、海報尺寸、連結、歌曲、來源、搜尋與 API 資料。
- 使用既有響應式 CSS，沒有新增 JavaScript、導覽狀態或資料欄位。

## 本機驗收

- `npm run catalog:check`：223 項通過；`npm run check`：公開邊界、Lint、321 項單元測試、建置、Cloudflare dry-run 及 37 項 Playwright 測試通過，最終瀏覽器測試沒有重試。
- 完整及正式依賴稽核均為 0 項漏洞；既有 `fast-uri` 3.1.6 安全回歸通過。
- 1,977 份 API 與 v1.44.0 source 逐位元相同，沒有新增或移除。建置保留 1,991 份 HTML、3,981 份公開產物，dry-run 沒有 runtime bindings。
- Chromium 以前後各 24 個畫面量測四套有圖／無圖作品，包含短名稱與長名稱；尺寸為 1280、960、640、390、320px，另有 390px 無 JavaScript 閱讀。全部畫面沒有水平溢出、頁面錯誤或提前建立的 iframe，海報框高度、外部連結及歌曲數均保持一致。
- 《毛球權次郎》在 1280px 的資料至外連間距約由 86px 降至 24px，主題曲分區間距約由 90px 降至 48px；第一首歌曲由頁面約 542px 移至 438px，提前約 104px。
- 同一作品在 390px 的第一首歌曲約由 550px 移至 517px，提前約 33px；文字、來源日期與影片同意操作保留。人工檢視這兩個尺寸的前後截圖。
- 截圖採減少動態效果設定；既有 E2E 以一般動態設定驗證桌面／手機、鍵盤、搜尋、來源展開、影片同意、無 JavaScript、離線與 404 流程。本次間距變更沿用既有回歸測試。

## 發布界線

本機與分支建置結果不代表正式網站已更新。發布時仍須核對合併 SHA、CI、Cloudflare 實際版本與流量，以及 live routes、headers、API 與產物內容。本切片未驗證實體裝置、Safari、Firefox、Lighthouse／Core Web Vitals 或實際 YouTube 播放。
