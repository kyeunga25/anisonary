<div class="catalog-preview" aria-hidden="true">
  <div class="catalog-preview__disc"><span></span></div>

  <div class="catalog-preview__sheet">
    <header>
      <span>ANISONARY</span>
      <strong>2026 SUMMER</strong>
    </header>

    <div class="catalog-preview__title">
      <small>動畫歌典</small>
      <b>主題曲核對記錄</b>
    </div>

    <ol>
      <li>
        <span class="catalog-preview__type">OP 01</span>
        <span class="catalog-preview__track"><b>日文曲名</b><small>ARTIST / VOCALS</small></span>
        <span class="catalog-preview__state">已審閱</span>
      </li>
      <li>
        <span class="catalog-preview__type catalog-preview__type--ed">ED 01</span>
        <span class="catalog-preview__track"><b>エンディング</b><small>OFFICIAL SOURCE</small></span>
        <span class="catalog-preview__state">已審閱</span>
      </li>
      <li>
        <span class="catalog-preview__type">OP 02</span>
        <span class="catalog-preview__track"><b>主題歌タイトル</b><small>CROSS-CHECKED</small></span>
        <span class="catalog-preview__state">已審閱</span>
      </li>
    </ol>

    <footer>
      <span>日本語</span>
      <span>繁體中文</span>
      <span>FIRST-PARTY</span>
    </footer>
  </div>
</div>

<style>
  .catalog-preview {
    isolation: isolate;
    min-height: 25rem;
    padding: 2rem 0 1.25rem 3.5rem;
    position: relative;
  }

  .catalog-preview::before {
    background-image:
      linear-gradient(var(--color-border) 1px, transparent 1px),
      linear-gradient(90deg, var(--color-border) 1px, transparent 1px);
    background-size: 2rem 2rem;
    content: "";
    inset: 0 0 2rem 4.5rem;
    mask-image: linear-gradient(145deg, #000 10%, transparent 78%);
    opacity: 0.55;
    position: absolute;
    z-index: -2;
  }

  .catalog-preview__disc {
    aspect-ratio: 1;
    background:
      radial-gradient(circle, var(--color-accent) 0 5%, #f7b1ab 5.5% 12%, var(--color-ink) 12.5% 18%, transparent 18.5%),
      repeating-radial-gradient(circle, var(--color-ink) 0 2px, #14234f 3px 5px);
    border: 1px solid color-mix(in srgb, var(--color-ink) 70%, transparent);
    border-radius: 50%;
    box-shadow: 0 1.2rem 2.5rem color-mix(in srgb, var(--color-ink) 18%, transparent);
    left: 0;
    position: absolute;
    top: 0;
    width: min(18rem, 54%);
    z-index: -1;
  }

  .catalog-preview__disc span {
    background: var(--color-canvas);
    border-radius: 50%;
    height: 0.7rem;
    left: 50%;
    position: absolute;
    top: 50%;
    transform: translate(-50%, -50%);
    width: 0.7rem;
  }

  .catalog-preview__sheet {
    background: var(--color-surface);
    border: 1px solid var(--color-border-strong);
    box-shadow: 0.9rem 1rem 0 color-mix(in srgb, var(--color-accent) 17%, transparent);
    margin-left: auto;
    max-width: 28rem;
    padding: 1.1rem;
    transform: rotate(1.2deg);
  }

  .catalog-preview__sheet header {
    align-items: center;
    border-bottom: 2px solid var(--color-ink);
    display: flex;
    font-size: 0.62rem;
    justify-content: space-between;
    letter-spacing: 0.14em;
    padding-bottom: 0.65rem;
  }

  .catalog-preview__sheet header strong { color: var(--color-accent); }
  .catalog-preview__title { display: grid; gap: 0.12rem; padding: 1rem 0 0.8rem; }
  .catalog-preview__title small { color: var(--color-muted); font-size: 0.62rem; letter-spacing: 0.16em; }
  .catalog-preview__title b { font-size: clamp(1.25rem, 3vw, 1.8rem); letter-spacing: -0.04em; }

  .catalog-preview ol { border-top: 1px solid var(--color-border); list-style: none; margin: 0; padding: 0; }
  .catalog-preview li {
    align-items: center;
    border-bottom: 1px solid var(--color-border);
    display: grid;
    gap: 0.75rem;
    grid-template-columns: 3.6rem 1fr auto;
    padding: 0.8rem 0;
  }

  .catalog-preview__type {
    background: var(--color-accent);
    color: #fff;
    font-size: 0.62rem;
    font-weight: 800;
    letter-spacing: 0.08em;
    padding: 0.35rem 0.4rem;
    text-align: center;
  }

  .catalog-preview__type--ed { background: var(--color-ink); }
  .catalog-preview__track { display: grid; min-width: 0; }
  .catalog-preview__track b { font-size: 0.78rem; overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }
  .catalog-preview__track small { color: var(--color-muted); font-size: 0.5rem; letter-spacing: 0.08em; margin-top: 0.18rem; }
  .catalog-preview__state { color: var(--color-accent); font-size: 0.56rem; font-weight: 720; white-space: nowrap; }

  .catalog-preview footer { display: flex; flex-wrap: wrap; gap: 0.35rem; padding-top: 0.9rem; }
  .catalog-preview footer span { border: 1px solid var(--color-border-strong); color: var(--color-muted); font-size: 0.5rem; letter-spacing: 0.08em; padding: 0.22rem 0.38rem; }

  @media (max-width: 760px) {
    .catalog-preview { margin-inline: auto; min-height: 22rem; max-width: 34rem; padding-left: 2.5rem; }
    .catalog-preview__sheet { max-width: 25rem; }
  }

  @media (max-width: 420px) {
    .catalog-preview { min-height: 20rem; padding-left: 1.25rem; }
    .catalog-preview__disc { width: 52%; }
    .catalog-preview__sheet { padding: 0.85rem; }
    .catalog-preview li { gap: 0.5rem; grid-template-columns: 3.25rem 1fr; }
    .catalog-preview__state { grid-column: 2; }
  }
</style>
